pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "decompose_navigation_sample"
include(":androidApp")
include(":shared")